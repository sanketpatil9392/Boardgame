name: My First Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check Linux
        run: uname -a

      - name: Print Message
        run: echo "Hello Sanket"
